interface=${IFACE}
driver=nl80211
ssid=${SSID}
hw_mode=${HW_MODE}
channel=${CHANNEL}

# 802.11n (Wi-Fi 4)
ieee80211n=1
wmm_enabled=1

# Security
wpa=2
wpa_passphrase=${PASSPHRASE}
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP
ieee80211w=1
